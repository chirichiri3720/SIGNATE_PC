defaults: 
  - base

cv: true

n_trials: 10
n_startup_trials: 5 #最初の10回でランダムに探索
n_jobs: 1
storage: outputs/optuna_storage
study_name: tmp-${now:%Y-%m-%d}-${now:%H-%M-%S}
name: ExpOptuna

delete_study: false